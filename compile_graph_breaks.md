# Wan2.2 DiT -- torch.compile graph-break report

_Generated by `tools/compile_graph_break_probe.py`. Diagnostic only; no source was modified._

## Run configuration

- **torch**: 2.6.0+cu124
- **device**: cuda
- **FlashAttn2**: True
- **FlashAttn3**: False
- **arch**: lite (representative)
- **dim**: 512
- **num_heads**: 8
- **num_layers**: 2
- **seq_len**: 320
- **full_shapes**: False

## Known graph-break sources (static analysis)

| # | Location | Construct | Category | Where | Mitigation for per-block compile |
|---|----------|-----------|----------|-------|----------------------------------|
| 1 | model.py:445-457 | `List[Tensor]` comprehensions + dynamic pad/`cat` | dynamic python / data-dependent shapes | pre-amble | skipped (compile blocks, not forward) |
| 2 | model.py:449-452 | `torch.tensor([u.size(1)...])`, `torch.stack([... u.shape[2:]])` | tensor-from-pyshape | pre-amble | skipped |
| 3 | model.py:453 | `assert seq_lens.max() <= seq_len` | data-dependent assert | pre-amble | skipped |
| 4 | model.py:517 | `grid_sizes.tolist()` in `unpatchify` | tensor->python list | post-amble | skipped |
| 5 | model.py:462-469 | float32 `autocast` for time embedding | autocast region | pre-amble | skipped |
| 6 | model.py:39-66,51 | `rope_apply`: `grid_sizes.tolist()` -> data-dependent `reshape` (unbacked symint `Eq(seq,u0*u1*u2)`) + py loop | tensor->python / unbacked symint | IN-BLOCK | wrap in `torch._dynamo.disable` (eager island) |
| 7 | attention.py:79-80 | `flash_attention` varlen `u[:v]` dynamic slicing + `flash_attn_varlen_func` | data-dependent slice / opaque op | IN-BLOCK | wrap `attention` in `torch._dynamo.disable` |

## Probe results (dynamic)

```

## Probe 1 -- whole WanModel.forward (enumerate breaks)
[whole model] graphs=9  graph_breaks=8  ops_captured=92
  break #1: Could not guard on data-dependent expression Eq(320, u0*u1*u2) (unhinted: Eq(320, u0*u1*u2)).  (Size-like symbols: none)

Caused by: x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(  # orkspace/Wan2.2/wan/modules/model.py:51 in rope_apply (_dynamo/utils.py:2586 in run_node)
For more information, run with TORCH_LOGS="dynamic"
For extended logs when we create symbols, also add TORCHDYNAMO_EXTENDED_DEBUG_CREATE_SYMBOL="u1,u2,u0"
If you suspect the guard was triggered from C++, add TORCHDYNAMO_EXTENDED_DEBUG_CPP=1
For more debugging help, see https://docs.google.com/document/d/1HSuTTVvYH1pTew89Rtpeu84Ht3nQEFTYhAX3Ypa_xJs/edit?usp=sharing

User Stack (most recent call last):
  (snipped, see stack below for prefix)
  File "/workspace/Wan2.2/wan/modules/model.py", line 243, in forward
    y = self.self_attn(
  File "/workspace/Wan2.2/wan/modules/model.py", line 146, in forward
    q=rope_apply(q, grid_sizes, freqs),
  File "/root/miniconda3/envs/wan/lib/python3.11/site-packages/torch/amp/autocast_mode.py", line 44, in decorate_autocast
    return func(*args, **kwargs)
  File "/workspace/Wan2.2/wan/modules/model.py", line 51, in rope_apply
    x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(

For C++ stack trace, run with TORCHDYNAMO_EXTENDED_DEBUG_CPP=1
For more information about this error, see: https://pytorch.org/docs/main/generated/exportdb/index.html#constrain-as-size-example
    model.py:243 in forward()   |  y = self.self_attn(
    model.py:146 in forward()   |  q=rope_apply(q, grid_sizes, freqs),
    autocast_mode.py:44 in decorate_autocast()   |  return func(*args, **kwargs)
    model.py:51 in rope_apply()   |  x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(
  break #2: Could not guard on data-dependent expression Eq(320, u0*u1*u2) (unhinted: Eq(320, u0*u1*u2)).  (Size-like symbols: none)

Caused by: x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(  # orkspace/Wan2.2/wan/modules/model.py:51 in rope_apply (_dynamo/utils.py:2586 in run_node)
For more information, run with TORCH_LOGS="dynamic"
For extended logs when we create symbols, also add TORCHDYNAMO_EXTENDED_DEBUG_CREATE_SYMBOL="u1,u2,u0"
If you suspect the guard was triggered from C++, add TORCHDYNAMO_EXTENDED_DEBUG_CPP=1
For more debugging help, see https://docs.google.com/document/d/1HSuTTVvYH1pTew89Rtpeu84Ht3nQEFTYhAX3Ypa_xJs/edit?usp=sharing

User Stack (most recent call last):
  (snipped, see stack below for prefix)
  File "/workspace/Wan2.2/wan/modules/model.py", line 146, in forward
    q=rope_apply(q, grid_sizes, freqs),
  File "/root/miniconda3/envs/wan/lib/python3.11/site-packages/torch/amp/autocast_mode.py", line 44, in decorate_autocast
    return func(*args, **kwargs)
  File "/workspace/Wan2.2/wan/modules/model.py", line 51, in rope_apply
    x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(

For C++ stack trace, run with TORCHDYNAMO_EXTENDED_DEBUG_CPP=1
For more information about this error, see: https://pytorch.org/docs/main/generated/exportdb/index.html#constrain-as-size-example
    model.py:146 in forward()   |  q=rope_apply(q, grid_sizes, freqs),
    autocast_mode.py:44 in decorate_autocast()   |  return func(*args, **kwargs)
    model.py:51 in rope_apply()   |  x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(
  break #3: Dynamic slicing on data-dependent value is not supported
    attention.py:79 in flash_attention()   |  k = half(torch.cat([u[:v] for u, v in zip(k, k_lens)]))
    attention.py:79 in <listcomp>()   |  k = half(torch.cat([u[:v] for u, v in zip(k, k_lens)]))
  break #4: Dynamic slicing on data-dependent value is not supported
    attention.py:80 in torch_dynamo_resume_in_flash_attention_at_79()   |  v = half(torch.cat([u[:v] for u, v in zip(v, k_lens)]))
    attention.py:80 in <listcomp>()   |  v = half(torch.cat([u[:v] for u, v in zip(v, k_lens)]))

## Probe 2 -- torch.compile(model, fullgraph=True) (the Flux idiom; expected to FAIL)
  Raised UserError (expected) -- whole-model fullgraph is not viable:
  Could not guard on data-dependent expression Eq(320, u0*u1*u2) (unhinted: Eq(320, u0*u1*u2)).  (Size-like symbols: none)
  
  Caused by: x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(  # orkspace/Wan2.2/wan/modules/model.py:51 in rope_apply (_dynamo/utils.py:2586 in run_node)
  For more information, run with TORCH_LOGS="dynamic"
  For extended logs when we create symbols, also add TORCHDYNAMO_EXTENDED_DEBUG_CREATE_SYMBOL="u1,u2,u0"
  If you suspect the guard was triggered from C++, add TORCHDYNAMO_EXTENDED_DEBUG_CPP=1
  For more debugging help, see https://docs.google.com/document/d/1HSuTTVvYH1pTew89Rtpeu84Ht3nQEFTYhAX3Ypa_xJs/edit?usp=sharing
  
  User Stack (most recent call last):
    (snipped, see stack below for prefix)
    File "/workspace/Wan2.2/wan/modules/model.py", line 490, in forward
      x = block(x, **kwargs)

## Probe 3 -- single WanAttentionBlock.forward (the real compile target)
[single block] graphs=7  graph_breaks=6  ops_captured=75
  break #1: Could not guard on data-dependent expression Eq(320, u0*u1*u2) (unhinted: Eq(320, u0*u1*u2)).  (Size-like symbols: none)

Caused by: x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(  # orkspace/Wan2.2/wan/modules/model.py:51 in rope_apply (_dynamo/utils.py:2586 in run_node)
For more information, run with TORCH_LOGS="dynamic"
For extended logs when we create symbols, also add TORCHDYNAMO_EXTENDED_DEBUG_CREATE_SYMBOL="u1,u2,u0"
If you suspect the guard was triggered from C++, add TORCHDYNAMO_EXTENDED_DEBUG_CPP=1
For more debugging help, see https://docs.google.com/document/d/1HSuTTVvYH1pTew89Rtpeu84Ht3nQEFTYhAX3Ypa_xJs/edit?usp=sharing

User Stack (most recent call last):
  (snipped, see stack below for prefix)
  File "/workspace/Wan2.2/wan/modules/model.py", line 243, in forward
    y = self.self_attn(
  File "/workspace/Wan2.2/wan/modules/model.py", line 146, in forward
    q=rope_apply(q, grid_sizes, freqs),
  File "/root/miniconda3/envs/wan/lib/python3.11/site-packages/torch/amp/autocast_mode.py", line 44, in decorate_autocast
    return func(*args, **kwargs)
  File "/workspace/Wan2.2/wan/modules/model.py", line 51, in rope_apply
    x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(

For C++ stack trace, run with TORCHDYNAMO_EXTENDED_DEBUG_CPP=1
For more information about this error, see: https://pytorch.org/docs/main/generated/exportdb/index.html#constrain-as-size-example
    model.py:243 in forward()   |  y = self.self_attn(
    model.py:146 in forward()   |  q=rope_apply(q, grid_sizes, freqs),
    autocast_mode.py:44 in decorate_autocast()   |  return func(*args, **kwargs)
    model.py:51 in rope_apply()   |  x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(
  break #2: Could not guard on data-dependent expression Eq(320, u0*u1*u2) (unhinted: Eq(320, u0*u1*u2)).  (Size-like symbols: none)

Caused by: x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(  # orkspace/Wan2.2/wan/modules/model.py:51 in rope_apply (_dynamo/utils.py:2586 in run_node)
For more information, run with TORCH_LOGS="dynamic"
For extended logs when we create symbols, also add TORCHDYNAMO_EXTENDED_DEBUG_CREATE_SYMBOL="u1,u2,u0"
If you suspect the guard was triggered from C++, add TORCHDYNAMO_EXTENDED_DEBUG_CPP=1
For more debugging help, see https://docs.google.com/document/d/1HSuTTVvYH1pTew89Rtpeu84Ht3nQEFTYhAX3Ypa_xJs/edit?usp=sharing

User Stack (most recent call last):
  (snipped, see stack below for prefix)
  File "/workspace/Wan2.2/wan/modules/model.py", line 146, in forward
    q=rope_apply(q, grid_sizes, freqs),
  File "/root/miniconda3/envs/wan/lib/python3.11/site-packages/torch/amp/autocast_mode.py", line 44, in decorate_autocast
    return func(*args, **kwargs)
  File "/workspace/Wan2.2/wan/modules/model.py", line 51, in rope_apply
    x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(

For C++ stack trace, run with TORCHDYNAMO_EXTENDED_DEBUG_CPP=1
For more information about this error, see: https://pytorch.org/docs/main/generated/exportdb/index.html#constrain-as-size-example
    model.py:146 in forward()   |  q=rope_apply(q, grid_sizes, freqs),
    autocast_mode.py:44 in decorate_autocast()   |  return func(*args, **kwargs)
    model.py:51 in rope_apply()   |  x_i = torch.view_as_complex(x[i, :seq_len].to(torch.float64).reshape(
  break #3: Dynamic slicing on data-dependent value is not supported
    attention.py:79 in flash_attention()   |  k = half(torch.cat([u[:v] for u, v in zip(k, k_lens)]))
    attention.py:79 in <listcomp>()   |  k = half(torch.cat([u[:v] for u, v in zip(k, k_lens)]))
  break #4: Dynamic slicing on data-dependent value is not supported
    attention.py:80 in torch_dynamo_resume_in_flash_attention_at_79()   |  v = half(torch.cat([u[:v] for u, v in zip(v, k_lens)]))
    attention.py:80 in <listcomp>()   |  v = half(torch.cat([u[:v] for u, v in zip(v, k_lens)]))

## Probe 4 -- per-block torch.compile smoke test (does the chosen strategy actually run?)
  `dynamic=False`, non-fullgraph. dynamo auto-breaks at the in-block blockers and runs them eagerly, so compile SUCCEEDS (unlike whole-model fullgraph). 'contained' additionally wraps rope_apply + attention with torch._dynamo.disable -- the production config: it makes those breaks intentional eager islands and avoids dynamo repeatedly fighting the unbacked-symint guards (the `evaluate_expr ... failed` churn above), which otherwise risks recompiles. Note: `disable` does NOT reduce the break COUNT -- each disabled call is a boundary -- it makes them clean.
  [plain] compiled+ran OK; matches eager=True (max_abs_diff=5.30e-03)
  [contained (disable)] compiled+ran OK; matches eager=True (max_abs_diff=5.30e-03)
```

## Conclusion

- **Whole-model `fullgraph=True` is not viable** as-is (Probe 2). The first hard failure is an unbacked-symint guard in `rope_apply` (`Could not guard on Eq(seq, u0*u1*u2)`), introduced by `grid_sizes.tolist()` feeding a data-dependent `reshape`; the varlen `u[:v]` slicing in `flash_attention` is a second class of blocker.
- **In non-fullgraph mode dynamo auto-breaks** at those points and continues, so both the whole forward and a single block *do* trace (Probes 1 & 3) -- just into several subgraphs around the breaks.
- The two in-block blockers (`rope_apply`, the flash-attn `attention` entry) live in the self-attention sub-region. They cannot be made fullgraph-clean by `allow_in_graph` (rope_apply is `@autocast`-wrapped and attention's varlen path is data-dependent); the right tool is `torch._dynamo.disable`, turning them into clean eager islands.
- **Probe 4 confirms per-block compile actually runs and matches eager** (both plain and contained). Containment via `disable` does not reduce the break *count* (each disabled call is a boundary) but removes the data-dependent-guard churn, which is the stable production config.
- **Why per-block, not whole-model:** compiling each `WanAttentionBlock` skips the break-heavy List[Tensor] pre/post-amble of `forward` entirely and lets the 30 identical blocks share one compiled artifact.
- **Next step (separate task, out of scope here):** wire a flag-gated `torch.compile(blk.forward, mode='max-autotune-no-cudagraphs', dynamic=False)` into `_configure_model`, with `rope_apply` + the `attention` entry wrapped in `torch._dynamo.disable`, then benchmark warmup vs steady-state.
